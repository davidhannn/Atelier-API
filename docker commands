docker run --name product-overview-db -p 5432:5432 -e POSTGRES_PASSWORD=123456 -d postgres

docker run --name pgadmin -e "PGADMIN_DEFAULT_EMAIL=name@example.com" -e "PGADMIN_DEFAULT_PASSWORD=admin" -p 5050:80 -d dpage/pgadmin4

docker run --rm --name product-overview-db -v
$HOME/Sync/animeDB/pgdata:/var/lib/postgresql/data -e
POSTGRES_PASSWORD=1234 -e POSTGRES_DB=your_database -p 6666:5432 myrepo/mypostgres:2

docker run --name product-overview-db -p 5432:5432 -v /Users/davidhan/data/pg:/var/lib/postgresql/data postgres

 docker run -d \
  --name=product-overview-db \
  --mount destination=/Users/davidhan/data/pg:/var/lib/postgresql/data \